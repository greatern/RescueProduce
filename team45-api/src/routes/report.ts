/*
import { Router } from 'express';
import ReportHandler from '../handlers/report.handler'; // Import the handler instance

const router = Router();

// POST /report - Endpoint to report fraud
router.post('/fraud', ReportHandler.reportFraud);

router.get('/', ReportHandler.getAllReports);

export default router;*/
